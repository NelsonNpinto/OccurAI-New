 <View style={appStyles.row}>
          {/* Mood Card */}
          <HealthMetricCard 
            title="Mood"
            emoji="😊"
            value={mood.score}
            unit="/100"
            subtitle={mood.status}
          />

          {/* Heart Rate Card */}
          <HealthMetricCard 
            title="Heart Rate"
            emoji="❤️"
            value={heartRateData.latest?.beatsPerMinute}
            unit="bpm"
            subtitle={heartRateData.latest?.beatsPerMinute ? 'Latest' : 'No Data'}
          />
        </View>

        {/* Bottom Health Metrics Row */}
        <View style={appStyles.row}>
          {/* Steps Card */}
          <HealthMetricCard 
            title="Steps"
            emoji="👣"
            value={todaySteps}
            subtitle={`${stepPercentage}% Completed`}
          />

          {/* SpO2 Card */}
          <HealthMetricCard 
            title="SpO2"
            emoji="💧"
            value={spo2Data.latest?.percentage ? `${Math.round(spo2Data.latest.percentage * 100)}` : null}
            unit="%"
            subtitle={spo2Data.latest?.percentage ? 'Normal' : 'No Data'}
          />
        </View>
